const [a, b, c, d] = [3, 10, 5, 7] // Criando 3 variaveis de forma rápida com destructuring


// Operadores BINARIOS pois utilizam 2 operandos para trazer o resultado
const soma = a + b + c + d // Operador INFIX
const subtração = a - b - c - d // Operador INFIX
const mult = a * b * c * d // Operador INFIX
const div = a / b / c / d // Operador INFIX
const modulo = a % b // Operador INFIX

d++ // Operador Unário POSTFIX 
++d // Operador Unário PREFIX
