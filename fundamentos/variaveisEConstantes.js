var a = 3
let b = 1
const c = 1

// Quando usamos var podemos redeclarar a mesma variavel e ela é subscrita
// Isso não ocorre com let. 
// Tudo que não tiver motivo para a mudar o ideal é criar constantes.
